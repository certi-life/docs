# CertiLife Docs 기여 가이드

이 저장소는 외부 사용자를 위한 공개 문서입니다. 문서를 수정할 때는 아래 원칙을 함께 지켜 주세요.

## 공개 근거와 범위

- 공개 홈페이지, 공개 로그인 화면 또는 별도로 승인된 자료만 근거로 사용합니다.
- 로그인 이후 확인하지 못한 메뉴·권한·업무 흐름은 추측하지 않습니다.
- 실제 고객·환자·직원 정보, 내부 URL, 계정, 키, 운영 지표를 포함하지 않습니다.
- 예시는 완전한 가상 데이터만 사용합니다.

## 링크 작성

- 외부 URL은 목적이 드러나는 설명형 하이퍼링크나 CTA로 제공합니다.
- `브라우저에서 https://...를 엽니다`처럼 URL 복사·붙여넣기를 요구하지 않습니다.
- 좋은 예: `[Manufacturer 로그인 화면을 엽니다](https://manufacturer.certi.life/signin)`
- 피해야 할 예: ``브라우저에서 `https://manufacturer.certi.life/signin`을 엽니다.``
- 링크 문구는 `여기`, `링크`보다 `Hospital 로그인`, `개인정보처리방침`처럼 목적지를 설명해야 합니다.
- URL 문자열 자체가 필요한 코드 예제만 fenced code block 안에서 예외로 취급합니다.

## 검색 가능한 문서 작성

- 제목과 `description`에 사용자가 실제로 찾을 제품명·문제·행동 용어를 사용합니다.
- 긴 문서는 작업 또는 문제 단위의 `##` 제목으로 나눕니다.
- 같은 의미를 지나치게 다른 표현으로 숨기지 말고, 필요한 경우 본문에서 일반적인 동의어를 함께 씁니다.
- 공개 문서만 검색 인덱스에 포함되도록 유지합니다.

## 검증

```bash
npm run docs:check
npm run typecheck
npm run build
npm run search:check
```

`search:check`는 production build 이후 실행해야 합니다. 모든 검사를 통과한 뒤 실제 모바일 화면, 검색 결과 이동, 변경한 외부 링크를 확인합니다.
