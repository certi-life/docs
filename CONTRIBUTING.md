# CertiLife Docs 기여 가이드

이 저장소는 외부 사용자를 위한 공개 문서입니다. 문서를 수정할 때는 아래 원칙을 함께 지켜 주세요.

## 공개 근거와 범위

- 공개 홈페이지, 공개 로그인 화면, 실제 제품 화면 또는 제품 소스 코드로 확인한 내용과 별도로 승인된 자료만 근거로 사용합니다.
- 실제 화면 또는 소스로 확인한 로그인 이후의 메뉴·버튼 이름과 기능 설명은 공개할 수 있습니다. 확인하지 못한 메뉴·권한·업무 흐름은 추측으로 쓰지 않습니다.
- 실제 고객·환자·직원 정보, 내부 URL, 계정, 키, 권한 구조의 세부, 운영 지표를 포함하지 않습니다.
- 예시는 완전한 가상 데이터만 사용합니다.

## 화면별 안내 (`docs/studio/screens/`)

화면별 안내는 사람이 읽는 사용 설명서이면서 Studio 안내 챗봇의 학습자료입니다. 챗봇은 사용자가 보고 있는 화면의 메뉴·창 이름을 전달받아 같은 문자열이 적힌 문서를 검색하고, 찾은 문장을 거의 그대로 말합니다.

**구조**

- 폴더와 사이드바는 Studio 왼쪽 메뉴의 위계를 그대로 따릅니다. 메뉴 1개 = 1쪽이고, 아직 자세히 쓰지 못한 메뉴도 개요 쪽은 만들어 둡니다.
- 페이지 제목은 `메뉴 이름 - 그 화면에서 하는 일` 형식으로 씁니다. 예: `작동시간 - 챗봇이 쉬는 시간 정하기`
- 첫 문단은 그 화면이 무엇을 하는 곳인지, 방법이 여러 가지라면 "방법은 N가지입니다"부터 말합니다. 챗봇이 "어떻게 해요?"에 바로 답할 수 있는 요약이 됩니다.
- 첫 `##` 앞에 `**위치:**` 줄을 둡니다(필수, `content:audit:check`가 검사). 화면에 보이는 메뉴 경로를 `챗봇 > 설정 > AI 설정`처럼 라벨 그대로 적습니다.
- 본문은 기능 나열이 아니라 **하려는 일** 기준으로 나눕니다. `## 이럴 때 사용하세요`(하고 싶은 일 → 섹션 표) 다음에 `## 첫 인사 메시지 바꾸기`처럼 목적형 제목을 쓰고, 번호 단계로 설명합니다.
- 특정 패널·모달을 다루는 섹션은 제목 바로 아래에 그 창까지의 `**위치:**` 줄을 한 번 더 적습니다.
- 주의할 점은 `:::caution`, 요령은 `:::tip`으로 분리하고, 필요하면 `## 권장 운영법`, `## 함께 보기`로 마칩니다.

**표현**

- 옆자리 동료에게 설명하듯 씁니다. "~할 수 있습니다"를 늘어놓지 말고, 왜 필요한지와 어떤 순서로 하는지를 말합니다. 헷갈리기 쉬운 지점(저장 방식, 이름이 비슷한 기능)은 먼저 짚어 줍니다.
- 메뉴·버튼 이름은 띄어쓰기까지 화면과 똑같이 적고 백틱으로 감쌉니다. 의역하지 않습니다.
- 같은 화면이 위치에 따라 다른 이름으로 보이면(예: 메뉴는 `AI 학습자료 설정`, 화면 제목은 `AI 학습자료 관리`) 두 이름을 모두 본문에 적습니다.
- beta이거나 조건부로만 보이는 기능, 조직의 이용 범위에 따라 보이지 않는 메뉴는 그 조건을 적습니다. 내부 플래그 이름이나 환경 변수는 적지 않습니다.
- 화면에 "준비 중"이나 샘플 데이터로 표시되는 기능은 동작을 약속하지 않습니다.
- 라벨은 Studio 소스 코드 또는 실제 화면에서 확인하고, 확인한 파일 경로를 PR 설명에 적습니다. 확인하지 못한 동작은 쓰지 않습니다.
- 스크린샷은 출시된 화면에서 가상 데이터로만 촬영하고, alt 텍스트에 화면 이름과 주요 라벨을 적습니다. 이미지에만 있는 정보를 만들지 않습니다.

## 링크 작성

- 외부 URL은 목적이 드러나는 설명형 하이퍼링크나 CTA로 제공합니다.
- `브라우저에서 https://...를 엽니다`처럼 URL 복사·붙여넣기를 요구하지 않습니다.
- 좋은 예: `[Manufacturer 로그인 화면을 엽니다](https://manufacturer.certi.life/signin)`
- 피해야 할 예: ``브라우저에서 `https://manufacturer.certi.life/signin`을 엽니다.``
- 링크 문구는 `여기`, `링크`보다 `Hospital 로그인`, `개인정보처리방침`처럼 목적지를 설명해야 합니다.
- URL 문자열 자체가 필요한 코드 예제만 fenced code block 안에서 예외로 취급합니다.

## 검색 가능한 문서 작성

- 제목과 `description`에 사용자가 실제로 찾을 제품명·문제·행동 용어를 사용합니다.
- 긴 문서는 작업 또는 문제 단위의 `##` 제목으로 나눕니다.
- 같은 의미를 지나치게 다른 표현으로 숨기지 말고, 필요한 경우 본문에서 일반적인 동의어를 함께 씁니다.
- 공개 문서만 검색 인덱스에 포함되도록 유지합니다.

## 검증

```bash
npm run docs:check
npm run typecheck
npm run markdown:generate && npm run markdown:check
npm run content:audit:check
npm run ai-eval:check
npm run test:ai
npm run build && npm run markdown:build-check && npm run search:check
```

`markdown:generate`는 페이지별 클린 마크다운(`static/guide/**.md`)과 AI 학습용 단일 파일 `static/llms-full.md`를 함께 만듭니다. `llms-full.md`는 Studio 학습자료 화면에 같은 이름으로 다시 올려 덮어쓰는 방식으로 운영하므로 파일명을 바꾸지 않습니다.

`search:check`는 production build 이후 실행해야 합니다. 모든 검사를 통과한 뒤 실제 모바일 화면, 검색 결과 이동, 변경한 외부 링크를 확인합니다.
