---
title: 콜봇 시작하기 - 전화를 받는 AI 상담원 준비하기
description: Studio 콜봇 메뉴에서 에이전트 만들기, 도구와 지식 베이스 연결, 조직 인증, 번호 발급과 연결, 통화 확인까지 콜봇이 실제 전화를 받게 하는 순서와 콜봇 메뉴 구성을 목적별로 안내합니다.
sidebar_label: 시작하기
---

# 콜봇 시작하기 - 전화를 받는 AI 상담원 준비하기

`콜봇`은 전화 문의를 AI 상담원(에이전트)이 받도록 만드는 곳입니다. 채팅의 [챗봇](../chatbot/overview)과 같은 역할을 전화에서 합니다. 콜봇이 실제 전화를 받으려면 **에이전트를 만들고 → 도구와 지식을 넣고 → 조직 인증을 받고 → 번호를 발급해 에이전트에 연결**해야 합니다. 왼쪽의 `시작하기`를 열면 이 순서와 지금까지의 진행 상태를 보여 줍니다.

**위치:** 콜봇 > 시작하기

:::info[조직의 이용 범위에 따라 보입니다]
`콜봇` 메뉴는 조직의 이용 범위에 따라 보이지 않을 수 있습니다. 또 일부 기능은 화면에 "아직 준비되지 않았습니다"로 표시될 수 있으며, 그 경우 해당 기능은 아직 쓸 수 없습니다. 필요하다면 조직의 CertiLife 담당자 또는 [도입 문의](https://certi.life/contact)로 확인하세요.
:::

## 실제 전화를 받기까지

`콜봇 시작하기` 화면의 단계를 그대로 따라가면 됩니다. 화면에는 "조직을 만들고 AI로 콜봇을 만들어, 실제 전화를 받게 하기까지 6단계입니다."라고 적혀 있고, 단계 목록은 아래 일곱 개입니다. 단계마다 자주 막히는 지점을 함께 적었습니다.

| 단계 | 하는 일 | 자주 막히는 지점 | 안내 |
| --- | --- | --- | --- |
| 1. `조직 만들기` | 화면 위 배너의 `조직 생성`을 누릅니다. 한 번만 하면 됩니다 | 조직 이름은 등록된 회사명으로 자동 설정됩니다 | |
| 2. `AI로 에이전트 만들기` | 업종과 맡길 일을 고르면 AI가 응대 내용 초안을 씁니다 | AI가 쓴 내용은 초안입니다. 영업시간·가격은 꼭 확인해 고치세요 | [에이전트 만들기](./agents) |
| 3. `에이전트에 도구 넣기` | 통화 종료, 통화 전환 같은 도구를 넣습니다 | 도구가 없으면 모르는 질문에 통화를 그냥 끝냅니다 | [에이전트 설정](./agent-settings#통화-중에-할-수-있는-일-늘리기) |
| 4. `지식 베이스 넣기` (`선택`) | 백과사전의 문서를 골라 담고 에이전트에 연결합니다 | 담기만 하면 쓰이지 않습니다. 에이전트에 연결해야 합니다 | [지식 베이스](./knowledge) |
| 5. `조직 인증받기` | 사업자 서류를 제출해 인증을 신청합니다 | 승인 전에는 번호 구매가 막혀 있습니다. 미리 신청하세요 | [조직 인증](./verification) |
| 6. `번호 발급하고 연결하기` | 번호를 구매하거나 연동하고 에이전트를 지정합니다 | 번호에 에이전트를 연결하지 않으면 전화가 와도 받지 않습니다 | [번호 관리](./phone-numbers) |
| 7. `전화 걸어 결과 확인하기` | 연결한 번호로 직접 전화해 보고 통화 기록을 확인합니다 | 통화 기록을 보면서 응대 내용을 계속 다듬습니다 | [전화상담](../phone-counsel) |

진행 표시(`0 / 6 완료`처럼 보입니다)는 필수 여섯 단계만 셉니다. 4번 `지식 베이스 넣기`에는 `선택`이 붙어 있어 세지 않습니다. 지식 베이스를 건너뛰어도 콜봇은 동작하지만, 넣지 않으면 우리 조직의 정보가 아니라 일반적인 답변만 합니다. `지금 하실 일:` 뒤에는 다음에 할 필수 단계가 `도구 넣기`, `번호 연결`처럼 짧은 이름으로 표시됩니다.

완료 표시는 실제 데이터로 판단합니다. 단계를 마쳤는데 표시가 그대로라면 오른쪽 위 `진행 상태 새로고침`을 누릅니다. 에이전트를 고친 뒤 실제 통화에 반영하는 `배포`는 이 단계 목록에 없습니다. 배포는 [에이전트 설정](./agent-settings#시험하고-배포하기)에서 따로 확인하세요.

## 메뉴 구성

**위치:** 콜봇

| 그룹 | 메뉴 | 하는 일 | 화면 안내 |
| --- | --- | --- | --- |
| `시작` | `시작하기` | 단계별 안내와 진행 상태 | 이 페이지 |
| `AI 상담원 만들기` | `에이전트` | AI 상담원을 만들고 설정하고 배포합니다 | [에이전트 만들기](./agents), [에이전트 설정](./agent-settings), [플로우 편집](./flow-editor) |
| | `보이스` | 쓸 수 있는 목소리를 찾아 미리 듣습니다 | [보이스](./voices) |
| | `도구` | 우리 시스템을 호출하는 API 도구를 만듭니다 | [도구](./tools) |
| | `매뉴얼` | 상황별 응대 절차를 작성합니다 | [매뉴얼](./manuals) |
| | `지식 베이스` | 에이전트가 참고할 문서를 담습니다 | [지식 베이스](./knowledge) |
| `캠페인` | `캠페인` | 목록의 번호로 콜봇이 전화를 겁니다 | [캠페인](./campaign) |
| `설정` | `번호 관리` | 번호를 구매·연동하고 에이전트에 연결합니다 | [번호 관리](./phone-numbers) |
| | `조직 인증` | 번호 구매에 필요한 사업자 인증을 신청합니다 | [조직 인증](./verification) |

조직의 이용 범위에 따라 `AI 상담원 만들기` 그룹에 [`DTMF 메뉴`](./dtmf-menus)(전화 키패드로 분기하는 ARS 메뉴 트리)가, `설정` 그룹에 [`사용자 관리`](./users)(상담사 번호 등록과 그룹 관리)가 함께 보일 수 있습니다.

화면 위에 배너가 보이면 먼저 처리해야 할 일이 있다는 뜻입니다.

| 배너 | 뜻 | 할 일 |
| --- | --- | --- |
| "콜봇 조직 연결이 필요합니다" | 이 회사의 콜봇 전용 조직이 아직 없습니다 | `조직 생성`을 누릅니다 |
| "조직 키 발급 대기 중입니다" | 조직을 만드는 중입니다. 발급이 끝나면 자동으로 활성화됩니다 | 기다립니다. 그동안 일부 기능이 제한될 수 있습니다 |
| "조직 인증이 필요합니다", "조직 인증이 반려되었습니다" | 번호 구매 같은 일부 기능은 인증 뒤에 쓸 수 있습니다 | `조직 인증하기`로 [조직 인증](./verification)을 신청합니다 |

## 하고 싶은 일로 찾기

| 하고 싶은 일 | 가야 할 화면 |
| --- | --- |
| 전화를 받는 AI 상담원을 새로 만들고 싶어요 | [에이전트 만들기](./agents) |
| 목소리, 인사말, 응대 내용을 바꾸고 싶어요 | [에이전트 설정](./agent-settings) |
| 모르는 질문은 상담사에게 넘기게 하고 싶어요 | [에이전트 설정의 도구](./agent-settings#통화-중에-할-수-있는-일-늘리기) |
| 우리 회사 자료로 답하게 하고 싶어요 | [지식 베이스](./knowledge) |
| 고친 내용이 실제 통화에 반영되지 않아요 | [에이전트 설정의 배포](./agent-settings#시험하고-배포하기) |
| 전화번호를 새로 받거나 기존 번호를 쓰고 싶어요 | [번호 관리](./phone-numbers) |
| 번호 구매가 막혀 있어요 | [조직 인증](./verification) |
| 여러 고객에게 안내 전화를 걸고 싶어요 | [캠페인](./campaign) |
| 통화 내용과 녹음을 확인하고 싶어요 | [전화상담](../phone-counsel), [상담 내역](../conversation-history) |

## 공개하기 전에

AI 음성 응대는 글과 달리 고객이 듣고 바로 이해해야 합니다. 공개 전에 직접 전화를 걸어 듣기 쉬운 문장인지, 숫자와 시간을 정확히 말하는지, 다시 듣기와 사람 연결이 되는지 확인하세요. AI 상담원임을 첫 인사에서 밝히는 것도 잊지 마세요. 점검 항목은 [출시 체크리스트](../../launch-checklist)에 있습니다.

## 문제가 생기면

### `서버에 연결하지 못했습니다. 네트워크 상태를 확인하고 잠시 후 다시 시도해주세요.`

콜봇 화면(에이전트 목록, 지식 베이스, 번호 관리 등)이 콜봇 서버에 연결하지 못했다는 뜻입니다. 설정이나 에이전트가 지워진 것은 아닙니다. [통계](../stats#문제가-생기면)와 [상담 내역](../conversation-history#문제가-생기면)의 전화 기록 화면에서도 같은 문구가 나옵니다.

1. 다른 사이트가 열리는지 보고 인터넷 연결을 확인합니다.
2. 문구 옆의 `다시 시도`를 누릅니다.
3. 그래도 같으면 브라우저를 새로고침합니다.

그래도 안 되면 화면의 문구, 연 화면 이름, 발생 시각을 적어 조직의 CertiLife 담당자에게 알립니다.

같은 자리에 아래 문구가 나올 때도 있습니다.

| 화면 문구 | 뜻 | 할 일 |
| --- | --- | --- |
| `요청 시간이 초과됐습니다. 네트워크 상태를 확인하고 잠시 후 다시 시도해주세요.` | 응답이 너무 늦어 기다리기를 멈췄습니다 | 잠시 뒤 `다시 시도`를 누릅니다 |
| `요청이 너무 잦습니다. 잠시 후 다시 시도해주세요.` | 짧은 시간에 요청이 몰렸습니다 | 1분쯤 기다린 뒤 다시 시도합니다 |
| `일시적인 서버 오류입니다. 잠시 후 다시 시도해주세요.` | 서버에 일시적인 문제가 있습니다 | 잠시 뒤 다시 시도하고, 계속되면 담당자에게 알립니다 |
| `요청 권한이 없습니다. 다시 로그인하거나 관리자에게 문의해주세요.` | 로그인이 만료되었거나 권한이 없습니다 | 로그아웃한 뒤 다시 로그인합니다. 그래도 같으면 관리자에게 권한을 요청합니다 |

### `상태를 확인하지 못했습니다. 완료 표시가 실제와 다를 수 있습니다.`

`콜봇 시작하기` 아래쪽에 "도구, 번호 상태를 확인하지 못했습니다. 완료 표시가 실제와 다를 수 있습니다."처럼 단계 이름과 함께 나옵니다. 그 단계의 진행 상태를 읽지 못해 체크 표시가 실제와 다를 수 있다는 뜻입니다. 설정 자체가 사라진 것은 아닙니다.

1. 오른쪽 위 `진행 상태 새로고침`을 누릅니다.
2. 그래도 남으면 해당 화면(예: `번호 관리`)을 직접 열어 실제 상태를 확인합니다.

### `조직 생성에 실패했습니다.`

배너의 `조직 생성`을 눌렀지만 콜봇 조직을 만들지 못했다는 뜻입니다. 잠시 뒤 `조직 생성`을 다시 누릅니다. 배너에 "조직 연결 기능이 아직 준비되지 않았습니다. (백엔드 준비 중)"이 보이면 아직 쓸 수 없는 상태이므로 조직의 CertiLife 담당자에게 문의합니다.

### 단계가 잠겨 있을 때

단계 목록에 자물쇠가 보이고, 그 단계를 열었을 때 "조직 생성을(를) 먼저 완료해주세요."처럼 표시되면 앞 단계가 끝나지 않은 것입니다. 적힌 단계를 먼저 마치고 `진행 상태 새로고침`을 누릅니다.
